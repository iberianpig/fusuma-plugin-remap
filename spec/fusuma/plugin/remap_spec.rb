# frozen_string_literal: true

RSpec.describe Fusuma::Plugin::Remap do
  it "has a version number" do
    expect(Fusuma::Plugin::Remap::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
